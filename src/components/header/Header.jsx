// Dependencies
import React from 'react';

// Styles
import styles from './Header.scss';

function Header() {
  return (
    <header className={styles.appHeader}>
      <h1>Push Reminders</h1>
    </header>
  );
}

export default Header;
